// Generated by CoffeeScript 1.7.1
var americano, options;

americano = require('americano');

options = {
  name: 'webdav',
  port: process.env.PORT || 9116,
  host: process.env.HOST || "127.0.0.1",
  root: __dirname
};

if (!module.parent) {
  americano.start(options, function(app) {
    return console.log("WebDAV Server listening on %s:%d within %s environment", options.host, options.port, app.get('env'));
  });
}

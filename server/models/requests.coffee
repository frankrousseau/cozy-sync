module.exports =
    cozyinstance:
        all: (doc) -> emit doc._id, doc
    webdavaccount:
        all: (doc) -> emit doc._id, doc
    contact:
        all: (doc) -> emit doc._id, doc
    alarm:
        all: (doc) -> emit doc._id, doc
    event:
        all: (doc) -> emit doc._id, doc
